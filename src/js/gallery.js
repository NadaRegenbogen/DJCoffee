document.addEventListener( 'DOMContentLoaded', function () {
    new Splide( '#image-carousel', {
      heightRatio: 0.5,
    } ).mount();
  } );
  
          new Splide( '.splide', {
    
              "type": "loop",
              "speed": 2000,
              "autoplay": true,
              "autoHeight": false,
          }).mount();